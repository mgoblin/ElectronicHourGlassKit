# What you need to install
on Debian Trixie

For gtk2 widgetset:
Install gtk2-engines-pixbuf, gnome-themes-extra,
libgail-common packages.

For qt5 widgetset:
Install libqt5pas-dev, lcl-qt5

For build executable:
lazbuild -B -r --bm=Release ehgk_editor.lpi
